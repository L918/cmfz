<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.lzz.dao.ArticleDao">
    <select id="selectIdAll" resultType="Article">
        select * from article where guru_id=#{guruId} limit #{page},#{rows}
    </select>
</mapper>